# Client App

Library catalog frontend built with React + Vite.

## Commands

```bash
npm install
npm run dev
npm run build
```

## Environment

Create `.env` from `.env.example` if you need a custom API URL.

```bash
VITE_API_BASE=http://localhost:4000/api
```
